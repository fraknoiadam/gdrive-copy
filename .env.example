# Development environment variables
# Copy this to .env and fill in your values

# Google OAuth Configuration
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here
VITE_GOOGLE_API_KEY=your_google_api_key_here

# Base URL for OAuth redirects (OPTIONAL)
# Leave empty for auto-detection based on current domain
# Only set this if you need to override the auto-detected URL
# VITE_BASE_URL=https://your-production-domain.com

# For development, auto-detection works fine (http://localhost:5173)
# For production deployment, you can either:
# 1. Leave VITE_BASE_URL empty (recommended) - auto-detects from current domain
# 2. Set VITE_BASE_URL to your production domain

# Examples:
# Development: VITE_BASE_URL=http://localhost:5173 (or leave empty)
# Cloudflare Pages: VITE_BASE_URL=https://your-app.pages.dev (or leave empty)
# Custom domain: VITE_BASE_URL=https://your-custom-domain.com (or leave empty)

# Note: Make sure to add your domain to Google Cloud Console:
# - Authorized JavaScript origins: your domain (e.g., https://your-app.pages.dev)
# - Authorized redirect URIs: your domain (e.g., https://your-app.pages.dev)
